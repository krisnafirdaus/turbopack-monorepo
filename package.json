{
  "name": "turbopack-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "eslint": "^8.40.0",
    "turbo": "^2.5.0",
    "typescript": "^5.0.4"
  },
  "packageManager": "pnpm@8.6.0"
}
